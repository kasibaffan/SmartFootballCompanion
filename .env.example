SECRET_KEY=change-me-to-a-long-random-string
# Optional: overrides the default local-appdata SQLite path (kept out of any
# cloud-synced folder on purpose - see app/config.py). Uncomment to point
# elsewhere, e.g. a real Postgres URL in production.
# DATABASE_URL=sqlite:///nexus.db
FLASK_DEBUG=0
